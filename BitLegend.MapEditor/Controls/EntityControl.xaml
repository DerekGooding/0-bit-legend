<UserControl x:Class="BitLegend.MapEditor.Controls.EntityControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:model="clr-namespace:BitLegend.MapEditor.Model" 
             d:DataContext="{d:DesignInstance Type=model:EntityData}"
             mc:Ignorable="d" 
             d:DesignHeight="30" d:DesignWidth="30"
             Background="Transparent">
    <Border BorderBrush="Red" BorderThickness="1">
        <TextBlock Text="{Binding EntityType}" 
                   HorizontalAlignment="Center" VerticalAlignment="Center"
                   Foreground="White"
                   ToolTip="{Binding EntityType}"/>
    </Border>
</UserControl>
