<UserControl x:Class="BitLegend.MapEditor.Controls.NumericUpDown"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="25" d:DesignWidth="100"
             Name="NumericUpDownControl">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>
        <TextBox x:Name="ValueTextBox" 
                 Grid.Column="0" 
                 Text="{Binding ElementName=NumericUpDownControl, Path=Value, UpdateSourceTrigger=PropertyChanged}"
                 VerticalContentAlignment="Center"
                 PreviewTextInput="ValueTextBox_PreviewTextInput"
                 LostFocus="ValueTextBox_LostFocus"/>
        <StackPanel Grid.Column="1" VerticalAlignment="Stretch">
            <Button x:Name="IncreaseButton" Content="▲" Click="IncreaseButton_Click" Height="Auto"/>
            <Button x:Name="DecreaseButton" Content="▼" Click="DecreaseButton_Click" Height="Auto"/>
        </StackPanel>
    </Grid>
</UserControl>